# SQL-Data-Warehouse
This project demonstrates the design and implementation of a Data Warehouse using Microsoft SQL Server. The solution follows a modern ETL architecture (Bronze → Silver → Gold) and uses multiple unclean CSV files from a real-world e-commerce dataset to build an analytics-ready star schema.
